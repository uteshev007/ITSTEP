#include <iostream>
#include <iomanip>  // библиотека манипул€торов дл€ cout
using namespace std;

/*
2) *ѕользователь вводит число. ¬ывести на экран факториал этого числа.
ќпределить максимальное число, факториал которого может хранитьс€ в переменной типа int.
**ѕредусмотреть и убрать возможность переполнени€ в программе
*/
int main()
{
  setlocale(LC_ALL, "RUS");
  unsigned long long n = 1; // пременна€ дл€ вычислени€ факториала
  unsigned long long nOld = 1; // предыдущее значение факториала
  int nMax = 100; // предел дл€ вычислени€ факториала
  for (int i = 2; i <= 100; i++) {
    n *= i; // n = n * i;
    cout << i << ": " << n << endl;
    if (nOld > n) {
      cout << "ќшибка вычислени€ факториала - "
        "переполение перемнной n" << endl;
      cout << "ћаксимум дл€ факториала: " << i - 1 << endl;
      break; // дальнейший счет бессмысленен
    }
    nOld = n; // обновл€ем старое значение факториала
  }
  cin.get(); cin.get();
}
