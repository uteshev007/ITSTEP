#include <iostream>
#include <iomanip>  // библиотека манипуляторов для cout
using namespace std;

/*
3a) *Вывести на экран прямоугольник с заданными
сторонами заданным символом.
*/
int main()
{
  setlocale(LC_ALL, "RUS");
  int a, b;
  char s;
  cout << "Vvedite razmery pryamougolnika:"
    " vysoty i shiriny\n";
  cin >> a >> b;
  cout << "Vvedite simvol\n";
  cin >> s;
  for (int i = 0; i < a; i++)
  {
    for (int g = 0; g < b; g++) {
      cout << s;
    }
    cout << endl;
  }
  cout << endl;
  for (int i = 0, g = 1; i < a * b; i++, g++)
  {
    cout << s;
    //if (g ==b) { cout << endl; g = 0; }
    if (g % b == 0) { cout << endl; }
  }
  cin.get(); cin.get();
}
