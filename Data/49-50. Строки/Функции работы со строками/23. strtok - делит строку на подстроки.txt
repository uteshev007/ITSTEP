#include <iostream>
#include <string.h>
using namespace std;

void main()
{
	setlocale(LC_ALL, "RUSSIAN");
	char name[100];
	cout << "¬ведите строку: ";
	cin.getline(name, 100);
	char word[50];
	cout << "¬ведите слово дл€ делени€: ";
	cin.getline(word, 50);
	char *find = strtok(name, word);// ƒелит строку на подстроки разделенных одним или несколькими символами
	if (find) {
		cout << find;
	}
	else {
		cout << "ќшибка: такого слова нет в вашем предложении!";
	}
	cin.get(); cin.get();
}