#include <iostream>
#include <string.h>
using namespace std;

void main()
{
	setlocale(LC_ALL, "RUSSIAN");
	char name[100];
	cout << "Введите вещественное число: ";
	cin.getline(name, 100);
	int res = atoi(name);// Переводит строку в целое число
	cout << res;
	cin.get(); cin.get();
}