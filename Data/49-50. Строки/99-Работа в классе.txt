#include <iostream>
#include <iomanip>
#include <time.h>
using namespace std;

int main()
{
  setlocale(LC_ALL, "Rus");
  srand(time(0));
  /*
  int a;
  // error C4700: использована неинициализированная локальная
  //   переменная "a"
  cout << a << endl; // error C4700
  */
  int mas2[3] = {10,20,30};
  int mas[3] = {1,2,3};
  cout << mas[0] << endl;
  mas[5] = 1234; // изменение значения mas2[0] через массив mas
  for (int i = 0; i < 10; i++) {
    cout << mas[i] << " ";
  }
  cout << endl;
  cout << mas[3] << endl; // выход справа за границу массива

  cout << "mas2: ";
  for (int i = 0; i < 3; i++) {
    cout << mas2[i] << " ";
  }

  cin.get(); cin.get();
}
