#include <iostream>
#include <stdio.h>

using namespace std;

int main()
{
	char str[500];
	gets_s(str); 

	int n;
	cin >> n;

	int i = 0;

	while (str[i] != '\0')
		i++; 
	for (int j = 0; j < n; j++)
		str[i + j] = 'a';

	str[i + n] = '\0';

	cout << str << endl;
}