#include <iostream>
using namespace std;
/*
8. ***ѕользователь вводит с клавиатуры врем€ в секундах,
прошедшее с начала дн€.
¬ывести на экран текущее врем€ в часах, минутах
и секундах.
ѕосчитать, сколько часов, минут и секунд осталось
до полуночи.
char, short, int, long, long long[-10**18 ; +10**18]
*/
int main()
{
  setlocale(LC_ALL, "Rus"); // установка русской локализации в консоли
  cout << "¬ведите врем€ в секундах: ";
  short Seconds; // дл€ секунд
  cin >> Seconds; // ввод времени в секундах
  // Seconds ==> H:M:S
  short H, M, S; // часы, минуты, секунды
  // 1)
  H = Seconds / 3600; // секунды ==> часы
  // 2)
  // (Seconds - H * 3600) - остаток от делени€ Seconds / 3600;
  //M = (Seconds - H * 3600) / 60; // секунды ==> минуты
  // Seconds % 3600 - остаток от делени€ Seconds / 3600;
  M = Seconds % 3600 / 60; // секунды ==> минуты
  // 3) получение оставшихс€ секунд после часов и минут
  //S = Seconds - H * 3600 - M * 60; // остаток в секундах
  S = Seconds % 60; // остаток в секундах
  // 4) ¬ывод в формате 1:2:3
  cout << "¬рем€ в „:ћ:— = ";
  cout << H << ":" << M << ":" << S << endl;

  cin.get(); cin.get(); // блокировка консоли
}
