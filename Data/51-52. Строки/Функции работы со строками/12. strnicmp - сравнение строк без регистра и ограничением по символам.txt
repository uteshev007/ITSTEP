#include <iostream>
#include <string.h>
using namespace std;

void main()
{
	setlocale(LC_ALL, "RUSSIAN");
	char name[100];
	cout << "Введите ваше имя: ";
	cin.getline(name, 100);
	// Сравнение первых 5 символов с каждой из строк БЕЗ УЧЁТА РЕГИСТРА ТОЛЬКО ДЛЯ ЛАТИНСКИХ БУКВ. Возвращает 
	//0 если строки равны,
	//-1 если первая строка меньше второй
	//1 если первая строка больше второй
	if (!strnicmp(name,"admin",5)) {
		cout << "Добро пожаловать, Администратор!"; 
	}
	else {
		cout << "Ошибка: неверный логин!";
	}
	
	cin.get(); cin.get();
}