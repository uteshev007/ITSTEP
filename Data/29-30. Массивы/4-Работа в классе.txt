#include <iostream>
#include <iomanip>  // библиотека манипул€торов вывода setprecision()
#include <time.h>  // time()
using namespace std;

/*
÷иклы, массивы

15. *Ќаписать программу, котора€ заполн€ет одномерный массив
из 10 элементов вещественными числами в диапазоне от 1 до 10
с двум€ знаками после зап€той.
а)**количество знаков после зап€той вводит пользователь.
*/

#define SIZE 10

int main()
{
  setlocale(LC_ALL, "Rus");
  srand(time(0));

  // иниц-ци€ массива списком значений {}, списочна€ инициализаци€
  double mas[SIZE]{1.2, 3.4, 5.12, -8.53, /*0,0,*/}; // —++
  double mas2[SIZE] = {0}; // массив заполн€етс€ нул€ми C/—++
  double mas3[SIZE]{};     // массив заполн€етс€ нул€ми —++

  /*for(int i = 0; i < SIZE; i++) {
    mas[i] = (double)(rand() % 10 + 1) / 100. + (rand() % 10 + 1);
    cout << mas[i] << " ";
  }
  cout << endl;*/

  double fMin, fMax;
  cout << "¬ведите диапазон [min;max]: ";
  cin >> fMin >> fMax;
  if(fMin > fMax) { // корректировка диапазона
    swap(fMin, fMax);
  }

  // перевод дробного диапазона в целые числа  *100
  int Min = fMin * 100;
  int Max = fMax * 100;

  for(int i = 0; i < SIZE; i++) {
    // делением на 100 возвращаемс€ в исходный диапазон
    mas[i] = (double)(rand() % (Max - Min + 1) + Min) / 100.;
    //mas[i] /= 100.;
  }
  //
  for(int i = 0; i < SIZE; i++) {
    cout << fixed << setprecision(2) << mas[i] << " ";
  }
  cout << endl;


  cin.get(); cin.get();
}
