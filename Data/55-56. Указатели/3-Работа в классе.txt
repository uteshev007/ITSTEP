#include <iostream>
#include <iomanip>
#include <time.h>
using namespace std;

#define SIZE 10

// указатели
int main()
{
  system("chcp 1251 > nul");
  srand(time(0));

  cout << "Введите длину строково массива: ";
  int size;
  cin >> size; // <ENTER>
  cin.ignore(); // убрать <ENTER> после cin >> size;
  // динамическая строк для ввода предложения с консоли
  //  str - указатель на динамический массив типа char
  //  str - указатель на строку
  char* str = new char[size];
  cout << "Введите строку: ";
  cin.getline(str, size); // чтение строки с консоли
  cout << "str: " << str << endl;
  //
  char* temp = str;
  int len = 0; // для подсчета длины строки
  while (*temp++ != '\0') { len++; }
  cout << "Длина строки: " << len << endl;
  //
  for (int i = 0; *(str + i) != '\0'; i++) {
    //cout << str[i] << " ";
    //cout << *(str + i) << " ";
    //cout << *(i + str) << " ";
    cout << i[str] << " ";
  }
  cout << endl;

  cin.get(); cin.get();
}
