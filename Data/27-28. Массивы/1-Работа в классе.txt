#include <iostream>
#include <time.h>
using namespace std;

/*
 ÷иклы, массивы
1. *Ќаписать, программу, котора€ вычисл€ет прибыль фирмы за 6 мес€цев.
ѕользователь вводит прибыль фирмы за каждый мес€ц
*/
int main()
{
  setlocale(LC_ALL, "Rus");

  //int mas[6]; // об€вление массива на 6 элементов - статический массив
  // mas[6]: [10][2][5][-10][7][-3]  <- значени€ элементов массива
  //           0  1  2    3  4   5   <- индексы элементов массива
  // mas[3][12] - двумерный массив 3 строки по 12 элементов
  // mas[10][3][12] - трехмерный массив глубиной на 10 элементов
  //                  по 3 строки в каждой по 12 элементов
  // mas[5][10][3][12] - четырехмерный массив
  // 
  // рабочий массив дл€ прибыли фирмы
  //int mas[6] = {10, 2, 5, -10, 7, -3}; // с иниц-цией элементов массива
  double mas[6] = {10, 2, 5, -10, 7, -3}; // с иниц-цией элементов массива
  // 
  //float mas2[10]; // массив по типу float c неинициализированными элементами - с мусорными данными
  
  cout << "¬ведите прибыль за 6 мес€цев: " << endl;
  for(int i = 0; i < 6; i++) {
    cout << i+1 << ": "; // номер элемента массива на экране
    cin >> mas[i]; // ввод дл€ i-го элемента массива
  }
  // вывод элементов массива
  for(int i = 0; i < 6; i++) {
    cout << mas[i] << " ";
  }
  cout << endl; // нонец строки элементов массива
  // можно и так - это плохой вариант
  //cout << mas[0] << " " << mas[1] << " " << mas[2];
  
  // сумму всех элементов массива - суммарна€ прибыль за 6 мес€цев
  //int summa = 0; // переменна€ дл€ накоплени€ суммы
  double summa = 0; // переменна€ дл€ накоплени€ суммы
  // цикл дл€ суммировани€ элементов массива
  for(int i = 0; i < 6; i++) {
    // summa = summa + mas[i];
    summa += mas[i]; // добавление i-го элемента массива в переменную суммы
  }
  // печать суммы после цикла суммировани€
  cout << "—уммарна€ прибыль = " << summa << " тенге" << endl;

  cin.get(); cin.get();
}
