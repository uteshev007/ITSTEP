#include <iostream>
using namespace std;

int main() {
	setlocale(LC_ALL, "RUSSIAN");

	// Если
	if (/* условие */) {
		// Действия если условие истина
	}
	// Иначе если
	else if (/* условие */) {
		// Действия если условие истина
	}
	// Иначе если
	else if (/* условие */) {
		// Действия если условие истина
	}
	// Иначе
	else {
		// Действия на все остальные варианты
	}

	cin.get();
}